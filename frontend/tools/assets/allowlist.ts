/**
 * Paths that are intentionally loaded through dynamic asset expressions.
 *
 * Keep entries narrowly scoped and explain the owning dynamic loader next to
 * each addition. This list is empty until the repository has a justified
 * dynamic asset use that cannot be resolved statically.
 */
export const DYNAMIC_ASSET_ALLOWLIST: readonly string[] = [];
